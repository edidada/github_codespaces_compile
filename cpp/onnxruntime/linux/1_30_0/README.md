# ONNX Runtime 1_30_0 on linux

ONNX Runtime is the production inference runtime for portable machine-learning models.

The script performs a real source compilation with the upstream CMake, Python, or Bazel entry point. It follows release v1.30.0; tests are excluded so CI measures compilation rather than the full validation suite.
