# Apache TVM 0_26_0 on linux

Apache TVM is an optimizing compiler stack for deep-learning models.

The script performs a real source compilation with the upstream CMake, Python, or Bazel entry point. It follows release v0.26.0; tests are excluded so CI measures compilation rather than the full validation suite.
