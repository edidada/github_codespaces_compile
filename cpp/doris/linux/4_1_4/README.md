# doris 4_1_4 on Linux

Doris is an MPP analytical database and data-warehouse engine. FE-only compilation keeps CI bounded while compiling the maintained Java control plane.

The script uses the official repository, a shallow recursive checkout, strict error handling, and the native build wrapper. It follows release 4.1.4.
