# MLX 0_32_2 on macos

MLX is Apple silicon-native machine-learning infrastructure and therefore intentionally targets macOS only.

The script performs a real source compilation with the upstream CMake, Python, or Bazel entry point. It follows release v0.32.2; tests are excluded so CI measures compilation rather than the full validation suite.
