# WebRTC 153 on linux

This uses Google's supported depot_tools, fetch, gclient, GN, and Ninja flow and builds the native webrtc target. Stable M153 is branch-head 8010; latest follows main. Tests and examples are disabled to bound CI compilation.
