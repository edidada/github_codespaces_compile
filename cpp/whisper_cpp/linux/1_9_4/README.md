# whisper_cpp 1_9_4 on linux

whisper.cpp is a portable native speech-recognition inference engine.

The script performs a real source compilation with the upstream CMake, Python, or Bazel entry point. It follows release v1.9.4; tests are excluded so CI measures compilation rather than the full validation suite.
