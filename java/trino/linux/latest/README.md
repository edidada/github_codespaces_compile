# trino latest on Linux

Trino is a distributed SQL query engine for warehouses and lakes. This compiles the main engine reactor while excluding tests and style gates.

The script uses the official repository, a shallow recursive checkout, strict error handling, and the native build wrapper. It follows the upstream default branch.
