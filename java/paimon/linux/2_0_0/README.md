# paimon 2_0_0 on Linux

Apache Paimon is a lake storage system. Building paimon-core validates its principal table and storage implementation.

The script uses the official repository, a shallow recursive checkout, strict error handling, and the native build wrapper. It follows release release-2.0.0.
