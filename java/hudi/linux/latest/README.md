# hudi latest on Linux

Apache Hudi is a transactional data-lake platform. The core module plus reactor dependencies validates the principal storage code without external clusters.

The script uses the official repository, a shallow recursive checkout, strict error handling, and the native build wrapper. It follows the upstream default branch.
