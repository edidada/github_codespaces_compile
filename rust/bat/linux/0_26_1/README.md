# bat 0_26_1 on linux

bat is a widely used syntax-highlighting replacement for cat.

This branch compiles the complete Cargo workspace in release mode on linux. It tracks the pinned release v0.26.1. The stable branch provides a reproducible industrial baseline while the latest branch detects upstream build regressions.
