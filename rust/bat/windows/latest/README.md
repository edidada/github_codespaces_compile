# bat latest on windows

bat is a widely used syntax-highlighting replacement for cat.

This branch compiles the complete Cargo workspace in release mode on windows. It tracks the upstream default branch. The stable branch provides a reproducible industrial baseline while the latest branch detects upstream build regressions.
