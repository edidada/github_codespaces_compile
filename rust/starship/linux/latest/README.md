# starship latest on linux

Starship is a popular cross-shell prompt implemented in Rust.

This branch compiles the complete Cargo workspace in release mode on linux. It tracks the upstream default branch. The stable branch provides a reproducible industrial baseline while the latest branch detects upstream build regressions.
