# starship 1_26_0 on macos

Starship is a popular cross-shell prompt implemented in Rust.

This branch compiles the complete Cargo workspace in release mode on macos. It tracks the pinned release v1.26.0. The stable branch provides a reproducible industrial baseline while the latest branch detects upstream build regressions.
