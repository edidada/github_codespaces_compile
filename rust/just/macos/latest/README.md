# just latest on macos

just is a cross-platform command runner used in many development workflows.

This branch compiles the complete Cargo workspace in release mode on macos. It tracks the upstream default branch. The stable branch provides a reproducible industrial baseline while the latest branch detects upstream build regressions.
