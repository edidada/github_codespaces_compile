# just 1_58_0 on linux

just is a cross-platform command runner used in many development workflows.

This branch compiles the complete Cargo workspace in release mode on linux. It tracks the pinned release 1.58.0. The stable branch provides a reproducible industrial baseline while the latest branch detects upstream build regressions.
