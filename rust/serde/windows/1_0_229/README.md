# serde 1_0_229 on windows

Serde is the ecosystem-standard serialization framework for Rust.

This branch compiles the complete Cargo workspace in release mode on windows. It tracks the pinned release v1.0.229. The stable branch provides a reproducible industrial baseline while the latest branch detects upstream build regressions.
