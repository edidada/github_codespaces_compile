# fd 10_5_0 on windows

fd is a popular high-performance command-line file finder.

This branch compiles the complete Cargo workspace in release mode on windows. It tracks the pinned release v10.5.0. The stable branch provides a reproducible industrial baseline while the latest branch detects upstream build regressions.
