# rayon 1_12_0 on linux

Rayon provides safe data-parallel iterators and thread-pool primitives.

This branch compiles the complete Cargo workspace in release mode on linux. It tracks the pinned release v1.12.0. The stable branch provides a reproducible industrial baseline while the latest branch detects upstream build regressions.
