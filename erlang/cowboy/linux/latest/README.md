# cowboy latest on Linux

Cowboy is the common Erlang HTTP and WebSocket server library used by OTP products.

The build uses the official repository and its native OTP, rebar, or make entry point. It follows the upstream default branch.
