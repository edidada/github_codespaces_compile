# delta_lake 4_4_0 on Linux

Delta Lake provides an ACID table format. Its official sbt wrapper supplies the repository-pinned Scala build.

The script uses the official repository, a shallow recursive checkout, strict error handling, and the native build wrapper. It follows release v4.4.0.
